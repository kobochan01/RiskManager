name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Build (electron-vite)
        run: npm run build

      - name: Package (electron-builder)
        run: npx electron-builder --win --publish never

      - name: Create GitHub Release & Upload Windows installer
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: pwsh
        run: |
          $exe = Get-ChildItem -Path dist -Filter "*.exe" | Select-Object -First 1 -ExpandProperty FullName
          gh release create "${{ github.ref_name }}" $exe `
            --title "RiskManager ${{ github.ref_name }}" `
            --generate-notes

  release-mac:
    runs-on: macos-latest
    needs: release-windows

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Build (electron-vite)
        run: npm run build

      - name: Build app directory (electron-builder)
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
        run: npx electron-builder --mac --dir

      - name: Ad-hoc sign the app
        run: |
          APP=$(find dist -name "*.app" -maxdepth 3 | head -1)
          codesign --deep --force --sign - "$APP"

      - name: Create DMG
        run: |
          APP=$(find dist -name "*.app" -maxdepth 3 | head -1)
          VERSION=$(node -p "require('./package.json').version")
          hdiutil create -volname "RiskManager" \
            -srcfolder "$APP" \
            -ov -format UDZO \
            "dist/RiskManager-${VERSION}-mac.dmg"

      - name: Upload macOS DMG to Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          dmg=$(find dist -name "*.dmg" | head -1)
          gh release upload "${{ github.ref_name }}" "$dmg"
